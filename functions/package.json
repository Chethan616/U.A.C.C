{
  "name": "uacc-gemini-proxy",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "eslint . || true",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:geminiProxy",
    "test": "node test_client.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.3.0",
    "@google-cloud/secret-manager": "^5.0.0"
  },
  "devDependencies": {
    "eslint": "^8.44.0"
  }
}
