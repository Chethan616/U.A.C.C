# requirements.txt
# This project is primarily Flutter (Dart) + Firebase + a Node.js Cloud Function.
# Example (uncomment as needed):
# google-cloud-firestore==2.16.0
# google-generativeai==0.8.3  # Optional: Python experiments with Gemini models (primary usage via Node Cloud Function)


# U.A.C.C optional Python tooling dependencies
# -------------------------------------------------------------
# Core project is Flutter + Firebase + Node.js Functions.
# Python is OPTIONAL for experiments (data analysis, ML, automation).
# Keep this file lean; only add what you actually use.
# -------------------------------------------------------------

# --- Observability / Admin Scripts (uncomment as needed) ---
# google-cloud-firestore==2.16.0    # Firestore admin scripts
# google-cloud-storage==2.18.0      # Storage maintenance scripts
# firebase-admin==6.5.0             # General admin utilities (Python)

#--ai api keys
#gemini 2.5 flash keys
